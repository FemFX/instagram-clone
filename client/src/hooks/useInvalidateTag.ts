// import { invalidateQueries } from "@redux-toolkit/query";
// import { useDispatch } from "react-redux";

// export const useInvalidateTag = (tag: string) => {
//   const dispatch = useDispatch();

//   const invalidate = () => {
//     dispatch(invalidateQueries(tag));
//   };

//   return invalidate;
// };
